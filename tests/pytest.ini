[tool:pytest]
# Disable bytecode cache generation in tests directory
addopts = -p no:cacheprovider --tb=short -v --cache-clear
python_files = test_*.py
python_classes = Test*
python_functions = test_*
cache_dir = /dev/null

# Ensure no cache directories are created
[pytest]
cache_dir = /dev/null