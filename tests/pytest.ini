[tool:pytest]
# Disable cache generation completely in tests directory
addopts = -p no:cacheprovider --tb=short -v
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Completely disable pytest cache
[pytest]
addopts = -p no:cacheprovider --tb=short -v